version: 0.0
os: linux
files:
  - source: .
    destination: /home/ubuntu/testing-cicd
hooks:
  AfterInstall:
    - location: env_setup/install_dependancies
      timeout: 300
      runas: root
  ApplicationStart:
    - location: env_setup/start_server
      timeout: 300
      runas: root
  